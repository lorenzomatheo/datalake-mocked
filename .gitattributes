# make all .ipynb be understood as python files
*.ipynb linguist-language=python

# make github not count the .html files in the repo stats
*.html linguist-vendored

# enforce LF line endings for YAML files to prevent shell breakage in GitHub Actions
*.yml text eol=lf
*.yaml text eol=lf