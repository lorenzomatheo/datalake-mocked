# utils

O módulo `utils` (ou utilities) serve para agrupar funções utilitárias ou
auxiliares que são amplamente utilizadas em diferentes partes do projeto, mas
que não pertencem diretamente a nenhum notebook ou processo específico.

Aquilo que pode ser reutilizado em outras partes do projeto, evitando repetição
de código e economizando tempo.
